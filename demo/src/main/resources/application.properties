# Travel4U - Configuración Principal
spring.application.name=demo

# --- CONFIGURACIÓN POSTGRESQL (PRODUCCIÓN) ---
spring.datasource.url=jdbc:postgresql://localhost:8180/sufrirIntegrador
spring.datasource.username=postgres
spring.datasource.password=conde-lemon
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# --- CONFIGURACIÓN DE JPA/HIBERNATE ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.hbm2ddl.auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Inicialización de datos
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
spring.sql.init.continue-on-error=true

# --- CONFIGURACIÓN DEL SERVIDOR ---
server.port=8081
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# --- CONFIGURACIÓN HTTP ---
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true

# --- CONFIGURACIÓN DE AMADEUS API ---
amadeus.api-key=GxhOKfHX2GTtZ9SHmWYAESyZDCytS1yL
amadeus.api-secret=L84cTUBjiWCHBoAH
amadeus.environment=test

# --- CONFIGURACIÓN DE LOGGING ---
logging.level.com.travel4u.demo=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
# --- CONFIGURACIÓN DE VUELOS ---
flight.default.origin=LIM
flight.default.destination=MAD

# --- CONFIGURACIÓN DE SCRAPER ---
scraper.node.enabled=false
scraper.html.enabled=false
scraper.html.auto=false